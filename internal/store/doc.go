// Package store contains CAW's JSON-backed repositories and filesystem path
// helpers. It is responsible for reading and writing wrapper state, profiles,
// and session mirrors, but it must not decide higher-level workflow behavior.
package store
